Run the install script to install the files to the drive.

Follow the instructions carefully to select the right GPU (it's simple, I promise)

Run ToggleGPU by pressing Super Key and typing ToggleGPU into the search, it should come up.

Enter sudo password to run the script to unbind the GPU

To remove the script, use the uninstall script or delete the directory and desktop file
